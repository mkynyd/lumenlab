# Design

## Motion

应用内部动效遵循克制动效原则：只用于解释状态变化，尊重 `prefers-reduced-motion`。

## Landing Page Exception

Landing 页是品牌第一印象与功能预览场景，允许使用装饰性滚动揭示、文字轮播与低对比环境渐变等氛围动效，但仍需响应 `prefers-reduced-motion`，并保证核心 CTA 与导航清晰可点击。环境渐变只能在 landing 使用，采用 transform/opacity 的非线性缓动，不得延迟首屏内容或用于文字填充。

## Authentication Surfaces

登录与注册属于「操作 + 解释」的双重场景。桌面端采用左右分区：左侧保持开放、无卡片包裹的认证表单，右侧使用冷中性色面板、品牌蓝与共享交互点阵呈现可手动切换的真实产品工作流；长注册表单滚动时，产品面板保持在首屏。移动端隐藏产品面板，把完整宽度留给认证任务。

产品说明只使用已经实现的项目资料、学习路径与成果导出能力，不使用虚构用户评价、统计指标或尚未接入的第三方登录。注册密码强度紧邻密码输入框，以四段强度条和逐项达标状态提供实时建议；建议不会暗中提高服务端现行的最低注册门槛。
