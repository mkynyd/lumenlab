# 数据结构复习

## 线性表

**顺序表**与*链表*是两种基本结构，详见 [课程主页](https://example.com/course)。

| 操作 | 顺序表 | 链表 |
| --- | --- | --- |
| 随机访问 | O(1) | O(n) |
| 头部插入 | O(n) | O(1) |

行内公式 $e = mc^2$ 与文字混排，中文标点不影响公式 $a_i + b_i$。

$$
\int_0^1 x^2 \, dx = \frac{1}{3}
$$

```python
def add(a, b):
    return a + b
```

脚注引用[^note]。

[^note]: 这是脚注内容，包含 **加粗** 文字。
