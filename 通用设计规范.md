# 通用设计规范

> 适用对象：AI 工作台类产品的聊天界面、色彩系统、按钮与通用控件规范
> 来源：从 LumenLab 工作台（学生端 AI 聊天 / 项目 / 学习）的实际实现中提炼，可直接复用于同类产品
> 范围：本文件不包含营销落地页（Landing）的视觉规范；落地页允许的氛围动效、环境渐变等仅限落地场景使用
> 配套：全局可访问性要求（44px 触控目标、键盘焦点、prefers-reduced-motion）已在本文各节内嵌，集中清单见「无障碍与交互细节」章节

---

## 1. 总体设计语言

现代科技极简风格。一句话概括：**中性色为主、单一蓝色强调、零描边的扁平面**。

### 1.1 五条硬性规则

1. **按钮与卡片无描边**。承担按钮或卡片角色的元素（含 `div`、`Link`、封装组件）在默认、hover、active、selected、focus-visible、disabled 任何状态下，都不得使用 `border`、`ring`、`outline` 或 `0 0 0 1px` 型 `box-shadow` 形成描边包裹。
2. **禁止深灰反馈**。选中态、悬浮态、菜单 focus 态不得使用近黑灰块（slate/zinc/neutral 800/900、亮度低于 0.2 的灰色）作为反馈色；必须用半透明极浅灰、低饱和色块、文字或图标权重表达。
3. **状态只做平面表达**。可点击、悬浮、选中、危险等状态通过背景填充、浅灰透明层、文字/图标权重、扁平 spotlight 或轻微色彩变化表达，不使用描边、不使用上浮位移强调。
4. **层级靠留白与内容层级**。卡片与区块之间用间距、背景明度差、内容密度、标题字重区分，不用边框分割，不做 hover 上浮。
5. **例外范围要窄**。输入框、表格、代码块、模态容器、菜单浮层、分隔线等「非按钮/非卡片」元素允许保留 1px 细线以保证可读性；判断标准是视觉角色，而不是底层标签。

### 1.2 其余原则

- **单一强调色**。全产品只有一个品牌色相（蓝），不引入第二个彩色强调；其余全部是中性色与语义色。任何新状态颜色先检查能否复用现有语义色。
- **语义色成对出现**。success / error / warning / info 每种颜色都配一个同色相低饱和 `muted` 变体，用于浅底 + 实色文字的徽章、横幅组合，避免大面积纯彩底。
- **动效克制**。动画只用于解释状态变化（进入、折叠、加载、按压），不使用装饰性持续动画；全部过渡遵守 `prefers-reduced-motion: reduce` 时降级为淡入或直接禁用。
- **卡片默认无投影**。阴影只保留在两个场景：需要「浮起」的悬浮小控件（如移动端浮航胶囊）用 `--shadow-pill`；输入类容器（如聊天输入坞）用 `--shadow-input`。面板、卡片、菜单一律无投影。
- **背景氛围克制**。点阵氛围背景与渐变光晕只用于认证、装饰性面板或大面空白区，不能与正文阅读竞争；工作台正文区保持纯色。

---

## 2. 色彩系统

### 2.1 组织方式

全部颜色用 CSS 自定义属性承载，语义命名（`--color-*`），再映射到 Tailwind v4 的 `@theme` 生成工具类。颜色一律使用 **OKLCH** 表达（明度 `L` 与中性度直观、可算对比度），只有品牌蓝等个别色带低饱和色相，中性色 Chroma 恒为 0。

主题结构：`:root` 定义浅色主题，`.dark` 覆盖为深色主题；同一语义令牌在两套主题下给出不同值，组件代码永远只引用语义令牌，不出现具体色值。

关键换算关系：

- 浅色主题的浅灰阶不是「白色 + 混黑」，而是白色与接近黑的深灰（`oklch(0.145 0 0)`，约 `#0a0a0a`）按透明比例叠加，保证与纯白背景叠后仍偏「冷中性」。
- 深色主题相反：底色为 `oklch(0.13 0 0)`（近 `#070707`）的墨色，表面层级用**白色透明层**（`oklch(1 0 0 / n%)`）在底色上叠出。
- 同一语义令牌在两套主题中的「用法」完全一致，只是数值翻转。

### 2.2 表面层级（Surface Hierarchy）

工作台用背景明度差区分层级，一共四层：

| 层级 | 语义 | 浅色主题 | 深色主题 | 用途 |
|---|---|---|---|---|
| 底层 | `--color-bg` | `oklch(1 0 0)` 纯白 | `oklch(0.13 0 0)` 墨色 | 页面底色，聊天正文区 |
| 表面层 | `--color-surface` / `--color-panel` | `oklch(0.985 0 0)`（约 `#fafafa`） | 白 5.5% 透明层 | 侧边栏、可折叠面板的底色 |
| 低对比填充 | `--color-panel-muted` | `oklch(0.965 0 0)`（约 `#f3f3f3`） | 白 7% 透明层 | 次级块：代码块、来源 chip、折叠的推理面板 |
| 控件底 | `--color-control` / `--color-control-menu` | 纯白 | `oklch(0.18)` / `oklch(0.19)` 提亮底 | 输入坞、菜单与弹窗面板 |

表面层需要给同一套 token 增加 hover / active 的透明层反馈：

| 令牌 | 浅色（叠在浅底上的深灰） | 深色（叠在墨底上的白） | 用途 |
|---|---|---|---|
| `--color-surface-hover` | 黑 5% | 白 8% | 可点行/项的悬浮态 |
| `--color-surface-active` | 黑 8% | 白 12% | 选中态（行、Tab、菜单项） |

> 项目资料场景另有 `project-*` 一组（`--color-project-surface-hover` 黑 6% / `--color-project-surface-active` 黑 10%，深色为白 9% / 白 13%），仅为与蓝色按钮区分交互语境，数值规律一致，新场景不必新建。

### 2.3 文字三层级

| 令牌 | 浅色 | 深色 | 用途 |
|---|---|---|---|
| `--color-text-primary` | `oklch(0.18 0 0)`（约 `#121212`） | `oklch(0.95 0 0)`（约 `#eeeeee`） | 正文、标题、选中项 |
| `--color-text-secondary` | `oklch(0.42 0 0)`（约 `#4d4d4d`） | `oklch(0.76 0 0)`（约 `#b1b1b1`） | 次要信息、描述、hover 后的说明 |
| `--color-text-tertiary` | `oklch(0.56 0 0)`（约 `#747474`） | `oklch(0.66 0 0)`（约 `#929292`） | 占位符、辅助说明、时间戳、折叠触发器 |

规则：正文与标题用 primary；hover 时 tertiary 文字升为 secondary，secondary 升为 primary（文字权重反馈，不使用描边）；12px 及以下辅助信息只允许 secondary / tertiary。

### 2.4 强调色（品牌蓝，单一色相）

| 令牌 | 浅色 | 深色 | 用途 |
|---|---|---|---|
| `--color-accent` | `oklch(0.5761 0.2508 258.23)`（约 `#006bff`） | `oklch(0.7308 0.1583 248.13)`（约 `#48aeff`） | 实心主按钮、链接、激活图标 |
| `--color-accent-hover` | `oklch(0.5151 0.2399 257.85)` | `oklch(0.78 0.13 248.13)` | 实心按钮悬浮，向更深的同色相移动 |
| `--color-accent-muted` | `oklch(0.9158 0.0473 245.12)`（约 `#cae7ff`） | `oklch(0.32 0.07 253)` | 状态底（如「学习中」徽章底、focus 填充的高对比版） |
| `--color-accent-soft` | `oklch(0.9458 0.0293 249.85)`（约 `#dfefff`） | `oklch(0.28 0.055 253)` | 更浅的强调底：选中开关（联网按钮）、焦点填充 |
| `--color-accent-contrast` | 纯白 | `oklch(0.13 0 0)` | 实心按钮上的文字色（反色） |

使用规则：

- 实心强调只用于主操作按钮与强激活图标，每屏最多一个实心主按钮。
- 选中「是开关型操作」用 `accent-soft` 底 + `accent` 前景表达，如输入坞里的联网搜索按钮、Skill 激活态。
- focus-visible 的可访问焦点用 `accent-soft` 背景填充实现，深色主题下它相对墨底约 3.5:1，超过非文本对比要求——这是刻意替代 ring 的方案（见 1.1 规则 1 与「无障碍」章）。

### 2.5 语义色

每种语义色两种形态：实色（文字/图形/进度条填充）与 `-muted`（浅底，用于横幅、徽章底）。

| 语义 | 实色（浅 / 深） | muted 底（浅 / 深） | 典型用途 |
|---|---|---|---|
| success | `oklch(0.575 0.145 160)` 约 `#00925a` / `oklch(0.72 0.14 150)` 约 `#5bbd74` | `oklch(0.925 0.055 158)` / `oklch(0.285 0.035 150)` | 已完成、已掌握、成功状态 |
| error | `oklch(0.575 0.195 25)` 约 `#d43538` / `oklch(0.69 0.18 25)` 约 `#f76560` | `oklch(0.925 0.052 25)` / `oklch(0.285 0.04 25)` | 错误横幅、破坏性操作文字、失败工具 |
| warning | `oklch(0.528 0.15 54.7)` 约 `#aa4c00` / `oklch(0.803 0.195 73.6)` 约 `#ffa600` | `oklch(0.96 0.064 90.5)` / `oklch(0.32 0.075 73.6)` | 警告、阻断原因条、待复习点 |
| info | `oklch(0.51 0.08 215)` / `oklch(0.72 0.06 215)` | `oklch(0.93 0.025 215)` / `oklch(0.29 0.022 215)` | 信息提示 |

组合公式：**`muted` 底 + 实色文字 + 同色 14px 图标**，构成横幅与状态徽章的标准样式；不透明度着色方案（如 `bg-error/10 text-error`）与之一致，二选一即可，项目中两种并存。

### 2.6 分隔线与边框

- `--color-border`：浅色 黑 14% / 深色 白 16% —— 仅用于需要更强辨识的容器细边（代码块、表格）。
- `--color-border-light`：浅色 黑 8% / 深色 白 10% —— 绝大多数细线场景：菜单浮层边、分隔线、输入坞边、折叠推理面板的左侧导引线。

**适用面清单**（即 1.1 规则 5 的全部例外）：输入框、代码块与表格容器、菜单/弹窗浮层面板、折叠面板的装饰性左线、`hr` 分隔线。卡片、行、按钮一律不出现边框。

### 2.7 遮罩与氛围

| 令牌 | 浅色 | 深色 | 用途 |
|---|---|---|---|
| `--color-overlay` | 黑 28% | 近纯黑 48% | 弹窗/抽屉的页面遮罩 |
| `--dot-grid-base` | `oklch(0.66 0 0 / 0.18)` | `oklch(0.68 0 0 / 0.16)` | 点阵背景的常态点 |
| `--dot-grid-active` | `oklch(0.42 0 0 / 0.72)` | `oklch(0.82 0 0 / 0.8)` | 交互点亮后的点 |

### 2.8 图表色

`--chart-1..5` 为纯中性灰阶（`oklch 0.87 → 0.269`），数据可视化中彩色只允许强调蓝与语义色出现在需要传达的状态上，其余序列用灰阶。落地页营销图除外。

---

## 3. 字体与排版

### 3.1 字体栈

- 正文与 UI：系统中文优先栈 `-apple-system, "Segoe UI", Roboto, "PingFang SC", "Microsoft YaHei", "Noto Sans SC", sans-serif`；英文与数字标题可换用 Figtree（本地打包 OFL 字体），中文字符回退 Noto Sans SC。
- 等宽：`ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace`，仅用于代码、token 数字、版本号。

### 3.2 字号阶梯

| 用途 | 字号 | 字重 | 备注 |
|---|---|---|---|
| 页面大标题 | 24–32px（`text-2xl`~`text-[2rem]`） | 600 | 负字距 -0.02em ~ -0.035em |
| 区块标题 / 卡片标题 | 16px（`text-base`） | 600 或 medium | 标题字距 tracking-tight |
| 正文 | 15–16px | 400 | 行高 1.6–1.78 |
| 次级文本（描述、次级行） | 13–14px | 400 | secondary 色 |
| 辅助 / 元信息 / 时间 | 11–12px | 400 | tertiary 色；列表元信息可用 11px |
| 按钮内文字 | 见按钮章节 | 500（medium） | 图标 16px |

行高：列表与控件 1.5 左右；正文区块（Markdown 正文）行高 1.75–1.78。聊天正文行高 1.75。

### 3.3 中文排版要点

- 正文 `overflow-wrap: anywhere`、不强制断词，长 URL 允许任意折行；标题允许任意位置断词。
- 数字型数据（token 数、时间）用 `tabular-nums` + 等宽字体的组合保持对齐。
- 所有正文抗锯齿：`-webkit-font-smoothing: antialiased`。

---

## 4. 圆角与阴影

### 4.1 半径令牌（本项目重定义了 Tailwind 内置比例，复用需注意）

| 令牌 | 值 | 典型归属 |
|---|---|---|
| `--radius-0` | 0 | — |
| `--radius-xs` | 4px | 行内代码角 |
| `--radius-sm` | 6px | 窄行内小元素 |
| `--radius-md` | **10px**（Tailwind 默认是 6px，被重定义） | 通用控件、列表行、徽章容器、小按钮内部裁角 |
| `--radius-lg` | **14px**（默认是 8px，被重定义） | 按钮、菜单面板 |
| `--radius-xl` | 18px | 卡片、弹窗面板 |
| `rounded-full` | 9999px | 胶囊 chip、图标圆钮、进度轨道 |
| 特例数值 | 20px（移动端底部弹窗顶角）、22px（用户气泡）、28px（输入坞） | 见聊天章节 |

经验规则：尺寸 ≤ 10px 的小元素上 `--radius-md`/`--radius-lg` 会趋近正圆，小尺寸控件需要时改用「限制上限的圆角」写法，例如 `rounded-[min(var(--radius-md),10px)]`（按钮 xs 尺寸的真实实现）。

> 由 Tailwind v4 `rounded-2xl/3xl` 由 `--radius`（10px）派生为 18px / 22px，用这些工具类前先按实际渲染尺寸核算是否过圆。

### 4.2 阴影

| 令牌 | 值 | 用途 |
|---|---|---|
| `--shadow-none` | none | 全部卡片、按钮、菜单面板、弹窗的默认值（刻意无投影） |
| `--shadow-pill` | 浅色 `0 1px 2px 黑5%, 0 4px 12px 黑4%`；深色加深一倍 | 移动浮航胶囊、悬浮圆形操作钮 |
| `--shadow-input` | 浅色 `0 1px 2px 黑4%, 0 8px 24px 黑3.5%`；深色加深 | 聊天输入坞这类「单个可读输入面」 |

阴影只允许出现在「悬浮于内容流之上」与「输入容器」两类元素；任何卡片/按钮/菜单如果使用阴影表达层级，一律删掉改用背景明度（2.2 节的表面层级表）。

---

## 5. 按钮规范

### 5.1 形态基因

无边框、无环、无描边（见 1.1 规则 1），圆角取 `--radius-lg`（14px），小尺寸改为「封顶圆角」（xs/sm 用 `min(--radius-md, 10px/12px)`）。带图标时图标尺寸随按钮尺寸走，与文字间距 `gap-1.5`（6px）。过渡只做 `background-color / color / opacity / transform`，时长 150ms；按下态用 `translate-y-px` 的 1px 微沉表达（active 反馈，比颜色更安静）。禁用态：`opacity-50` + 去除指针事件。

### 5.2 变体

| 变体 | 视觉 | 使用场景 |
|---|---|---|
| primary / default | 实心 `--color-accent` 底 + `--color-accent-contrast` 字；hover 换 `--color-accent-hover` | 主操作：发送、保存、创建。每屏一个 |
| secondary | 浅灰 `--color-surface` 底 + primary 文字 | 次操作，表单中的常规按钮 |
| ghost | 透明底，hover 叠 `interaction-hover` 浅灰、hover 文字加深 | 图标操作、工具栏、折叠触发器；最常见变体 |
| destructive / danger | `error` 10% 透明浅底 + `error` 实色文字（深色主题 20%），hover 换 `--color-error-muted` 实底 | 危险操作（删除、停止生成）。不做红色实心底 |
| outline | 无边框版：背景底，hover 叠浅灰（项目将 shadcn outline 改造为无描边） | 需要与 ghost 区分的弱按钮 |
| link | 纯文字，underline-offset hover 下划线，色用 `accent` | 行内文字操作 |

深灰、黑灰填充禁止出现在任何 hover / active / selected / focus 状态中（1.1 规则 2）。按钮的 aria-expanded 展开态使用 `interaction-active`（比 hover 深一档的浅灰）。

### 5.3 尺寸

| 尺寸 | 高 | 字号 | 图标 | 用途 |
|---|---|---|---|---|
| xs | 24px | 12px | 12px | 密度极高的分组工具 |
| sm | 28px | 13px（`text-[0.8rem]`） | 14px | 行内小操作、保存、折叠触发 |
| default / md | 32px | 14px（text-sm） | 16px | 默认表单操作 |
| lg | 36px | 14px | — | 空间充裕的强调操作 |
| icon（裸图标） | 32×32 | — | 16px | 桌面工具栏 |
| icon-sm / icon-xs | 28 / 24 | — | 14 / 12 | 紧凑行内图标 |
| icon-lg | 36×36 | — | — | 输入坞发送这类重点圆钮 |

横向内边距：默认 `px-2.5`，带图标时首尾图标内侧收窄到 `pl/pr-2`，保证图标与文字视觉居中。圆形按钮：聊天输入坞里的附件 / 发送 / 停止用正圆（`rounded-full`），尺寸 36px 起步，移动端强制 44px（见 6.6 触控）。

### 5.4 图标按钮与 Tooltip

纯图标按钮必须有可访问名称（`aria-label`），桌面端一律包 Tooltip（悬停提示，见「浮层控件」）。默认工具栏图标用 lucide 线性图标 `strokeWidth={2}`；激活态图标变重（`strokeWidth={2.5}`）作为次级反馈。

### 5.5 组合与胶囊（chip）

- 紧凑按钮组（ButtonGroup）：成员互相削掉邻边圆角拼合成一条（`rounded-l-none / rounded-r-none`），组内以 1px `--color-border-light` 分隔；用于「全选/上传/分类」这类一组同构操作，由分割而非独立卡片表达。
- 建议胶囊（chip）：`h-8` 圆角 `rounded-full`、`px-3`、`text-xs`，底色 `project-control`（约黑 5% 的灰），hover 转 `project-surface-hover` 并加深文字；用于快捷任务栏、跟随消息的追问按钮。
- 消息级小动作（如「保存为成果」）：ghost 文本按钮，`px-2 py-1`、`text-xs`、tertiary 起步 hover 转 primary。

### 5.6 焦点样式

按钮与链接的 `focus-visible` 用 `--color-accent-soft` 背景填充 + 文字转 primary（浅色主题约 3.5:1 对比，满足非文本 3:1）；**不用 ring 或 outline**。键盘导航焦点在列表项上同样用浅灰填充而非描边。鼠标点击时不显示焦点样式（`:focus-visible` 天然区分）。

---

## 6. 通用控件速查

### 6.1 输入类（允许细边框的例外）

- 内联输入（搜索、标题）：高 32px，圆角 10px（`rounded-[--radius-md]`），**无边框**，底色为 5% 级浅灰透明层（`interaction-hover`），focus 时加深为 `surface-active`（8% 级）作为聚焦反馈；占位文字 tertiary；禁用 50% 透明。
- 页面表单输入框（Input 组件）：高 32px、圆角 10px、无边框、底色 `interaction-hover` 填充、focus 换 `surface-active`，键盘焦点只靠底色变化与强调色光标，不加 ring（输入类细边框方案仅用于「坞」「面板输入」等容器化场景，见聊天输入坞）。
- 多行输入（textarea）：无边框无填充容器内嵌（聊天输入坞），`caret-color: accent` 作为焦点提示，输入反馈靠光标本身，不给 focus ring。
- 全局规则：输入、textarea、select 的 `focus-visible` 只设置强调色光标（`caret-color`），保持扁平外观。
- 密码输入默认掩码 + 提供文字显隐切换。

### 6.2 选择类（Select / 下拉菜单）

- 触发件可以是 ghost 按钮（工具栏场景，如模型选择）或 `secondary` 变体满宽按钮（表单场景）。
- 菜单浮层面板：`--color-control-menu` 底、1px `border-light` 细边、圆角 14px（`rounded-lg`）、**无投影**；选项行高 32–40px、圆角 10px（`rounded-md`）；选中项用 `interaction-active` 浅灰底 + 文字加深，图标选中用 accent；分组标题用 tertiary 12px。
- 菜单浮层是 1.1 规则 5 的例外，允许细边框；但面板内选项本身仍遵守无描边原则。

### 6.3 徽章与胶囊状态

- 徽章（Badge）：`h-5`、圆角接近胶囊（`rounded-4xl`，本项目为 26px）、`px-2`、`text-xs font-medium`。状态徽章一律「浅底 + 实色字」：如已掌握 `success-muted` 底 + `success` 字、学习中 `accent-muted` 底 + `accent` 字、新内容浅灰底 + secondary 字。
- 纯状态点：2px（`h-1.5 w-1.5`）实心圆点 + 文字，不加容器，如内容新鲜度徽标。
- 分隔菜单的分组名用「我的任务」式 12px tertiary 标签。

### 6.4 进度与加载

- 线性进度：轨道 `rounded-full`、高度 4px（sm）~6px（md），轨道色 `--color-ring-track`；填充色按语义：accent / success / warning / error，宽度过渡 300ms ease-out。
- 环形进度：stroke 3px，`--color-ring-track` 轨道 + `--color-accent` 填充。
- AI 等待：不用普通 Spinner 表现「模型在思考」，用思考小球（thinking-orbs 点阵动画，20px 常态 / 64px 大尺寸）置于**助手气泡内部**，配双行小字：`label`（12px medium primary）+ `detail`（12px tertiary）；三种速率 calm/normal/fast。fallback 为普通 spinner（16–28px）。
- 区块加载：Skeleton 用 `ring-track` / `surface-hover` 级灰块 + 对应元素圆角。

### 6.5 弹窗（Dialog）

- 遮罩 `--color-overlay`（浅色黑 28% / 深色近纯黑 48%）；面板 `control-menu` 底、1px `border-light`、圆角 18px（xl）、无投影。
- 进出场 150–200ms `cubic-bezier(0.23, 1, 0.32, 1)` + `zoom-in-95`。
- 移动端全屏式选项改用**底部弹窗**：贴底、顶部大圆角（20px）、`rounded-b-none`、无水平边框，底部加 `env(safe-area-inset-bottom)` 安全区；面板内选项整行高 44px、`justify-start`、左图标，选中用 `interaction-active` 底。
- 用户滚动长内容时弹窗面板本身可溢出滚动（modal 常规要求）。

### 6.6 触控与命中区

移动端（`pointer: coarse` 或 `max-width: 640px`）强制：按钮与可点元素最小 44×44px（全局 CSS 已兜底，`[data-slot="button"]` 与无 slot 按钮统一执行）；header/aside 内链接同样 44px。桌面端图标按钮最小 28px，带说明的按钮 32px。

---

## 7. 聊天界面规范（核心章节）

### 7.1 布局骨架

Chat 工作台是「全高 + 双栏 + 居中消息流」结构：

- 页面：`100dvh` 全屏单视图，无页面滚动，只有消息列内部滚动。
- 左导航（桌面）：独立栏 256px（收起 64px）；移动端为抽屉（`min(20rem, 85vw)`），用遮罩 + 主区右移 300ms 推开（缓动 `cubic-bezier(0.25, 1, 0.5, 1)`）。
- 主区：上为主消息流（内部滚动），底部固定输入坞，不随消息滚动。
- 消息流与输入坞共用内容列宽：**消息文本最大 `48rem`（768px）**；空态主视觉组合最大 `52rem`。消息区在窄屏只保留 4–6px 水平余量（`px-4 md:px-6`）。
- 普通正文的「可读列宽」上限 74ch 不适用于聊天内容：助手消息通栏排布（AI 回复不同于长文阅读），空态标题、项目说明等仍可套 74ch。

### 7.2 对话的两种形态

**空态（第一屏）**：内容垂直居中于消息流区（下缘余 `10vh`），构成「标题 + 一句话说明 + 输入坞」：

- 标题 26–32px（`text-[1.65rem] sm:text-[2rem]`）semibold，字距 `-0.035em`，文案一句到位的问句（如「今天想一起完成什么？」）；
- 说明 14px、tertiary、行高 24px、`max-w-md` 居中；
- 输入坞就是空态的主视觉，快捷键提示、附加控件都在坞内展开，不另起一行按钮区。

**对话态**：消息流按序排列；输入坞固定在底部并始终可见。两种形态共用同一个 `ChatInput` 组件（无边框位置切换），保证状态连贯。

### 7.3 消息形态（核心视觉决策）

本产品采用「混合布局」——它决定了聊天区的气质：

1. **用户消息：右对齐气泡**。无头像；`max-width: 85%（窄屏）/ 70%（≥640px）`；填充 `interaction-active`（浅色黑 8% / 深色白 12% 的浅灰，**不用品牌蓝**——蓝色实心气泡会造成「全屏彩色」喧嚣）；四角统一圆角 `22px`、无箭头尾巴；内边距 `px-4 py-2.5`；文字 primary，随 Markdown 轻渲染。
2. **助手消息：无气泡、通栏文档流**。消息宽度即内容列宽（`max-w-[48rem]`），AI 回复按 Markdown 文档排版直接铺在背景上（辅助信息上置、正文通栏、来源与摘要下置）；气泡样式只属于用户——由此 AI 回复与 UI 界面形成「正文纸面」与「操作面」的层级对比，同时避免回复过长时出现巨型色块。
3. **正文区**（`.chat-message-text`）：15–16px、行高 1.75、可折行；首尾消息组间距 `py-3 md:py-4`（组高 12–16px + 内部留白），消息间不再加分隔线。
4. **流式回显**：正文生成中，末尾跟一个闪烁的等宽光标（`▌`，1s blink，accent 色）；空内容阶段在气泡内部渲染状态行（见 7.5）。

### 7.4 助手消息的纵向信息架构

从下到上（即文档流顺序）依次是：**过程条 → 思考折叠 → 正文 → 来源托盘 → 动作区 → 摘要行**。可用信息与重量从「先过程后结论、先内容后出处」排列：

1. 过程条（可选）：AI 正在搜索/读网页/执行工具时显示在正文上方一行；结束后若产生工具调用则折叠为可回放的摘要条（工具数量 + 结果），可点击展开工具级详情（成功绿 / 失败红 / 进行中状态）。宽度占满整行、行高约 32px。
2. 思考折叠（可选）：模型「思考过程」默认折叠，触发词 12px tertiary（状态 12px + 旋转小图标）；展开后内容置于 `panel-muted` 圆角块（10px）内，12px、行高 relaxed、secondary 色。推理进行中显示「正在思考」+ 小球。
3. 正文：Markdown（见 7.6）。
4. 来源托盘（有检索时）：标题行「来源 · N」12px secondary + 一排 chip：`panel-muted` 底、圆角 10px、高约 28px、图标 12px（网页/文件/论文各自图标）、文字截断（`max-w-[18rem]`）中段省略；可点 chip（外部链接）hover 叠 `interaction-hover`。超过 5 条只显示前 5 条 + 「+N」残数 chip。
5. 动作区（可选）：正文之后允许的轻量动作以 12px ghost 文本按钮与 chip 出现：如「保存为成果」（展开后出现标题输入 + 类型选择 + 主按钮）、「引导我深入理解」等追问胶囊（`h-7` 圆角 10px、panel-muted 底、xs 字）。动作按钮一律在正文**下方**、独立一行，不悬浮在正文上。
6. 摘要行：全部完成后一行 12px tertiary：`使用 N 个工具 · M 个来源 · N,N tokens`（等宽数字）——过程结束后正文区恢复安静。

### 7.5 AI 过程与等待状态

- **等待必须有文案**。空阶段在正文位置渲染「状态行」：小球（20px，按动作切换形态：搜索/连接/编织）+ `label`（12px medium）+ `detail`（12px tertiary），如「等待模型响应 / 正在建立输出流」；文案统一名词短语，不出现「loading…」英文。
- 工具执行的行内状态按工具类型给中文映射：「正在搜索」「正在读取网页」「正在检索资料」「正在读取论文」等；流式过程中已执行工具逐一收起为绿色完成态。
- 生成完成后若使用了 N 个工具、M 个来源，正文结束后给摘要行（见 7.4-6），不打断阅读。
- 思考/计划过程长于常规时放进可折叠面板，避免消息顶部被状态行堆满；全部过程块文字 13px、secondary/tertiary 层级。

### 7.6 输入坞（Composer）

**形态**：单块「输入码头」——圆角 `28px`、背景 `--color-control`、1px `--color-border-light` 细边、柔和阴影 `--shadow-input`（输入类例外，见 4.2）、内边距 `p-1.5 sm:p-2`。坞内部不再嵌套卡片或填充条。

**结构**（桌面）：同一行内从左到右：附加文件、模型选择、Skill 选择、联网开关的图标组（分隔于输入区左侧）……输入框居中占满，最右发送/停止圆钮。

- textarea：无边框无背景（`bg-transparent`）、无 focus ring；16px 字号、24px 行高（保证中文阅读密度）；`min-height: 44px`，随输入最多长到 160px 后内部滚动；Enter 发送、Shift+Enter 换行；占位文字 tertiary（如「问点什么」）。
- 发送按钮：仅在有内容或附件时可点，primary 实心正圆（36px，移动端 44px）；生成中切换为停止按钮——形态不变（正圆、同位置），配色换 `error 10% 底 + error 图标`（destructive 浅底版），避免「发送/停止」跳位。
- 附加文件 chip：折叠在输入区上方一行（`text-xs` + 图标 12px），移除按钮为纯 x 图标，`max-w-56` 截断文件名。
- 输入被阻断时（如文件还在解析）：输入坞上方显示一条 12px 提示，`warning-muted` 底 + warning 文字（禁止输入则由 disabled 态接管，见 7.8）。
- 底部安全区：`padding-bottom: max(0.75rem, env(safe-area-inset-bottom))`。

**移动端**：单行「+ 附件 / 输入 / 发送」；模型、Skill、联网、思考深度全部收进底部弹窗（「对话选项」，见 6.5），整行 44–48px 可点、左图标；选中态 `interaction-active` 填充或 accent-soft 图标。

**模型 / Skill 触发器**（在坞内）：32px 高、`panel-muted` 底圆角复合按钮（模型选择 14px 圆角，图标型触发器收为 10px），图标 + 截断标签 + chevron；模型选择打开双分组菜单（模型 / 思考深度两段，各自 radio）。图标型开关（联网）默认 ghost，激活时变 `accent-soft` 底 + accent 图标 + 图标加重——「开关激活」用强调色而不用深灰。

### 7.7 Markdown 正文样式（AI 回复的画布）

正文容器 typography 同「3.2 字号阶梯 + 行高 1.75」；内容级排版：

- 标题 1/2 级下加 1px `border-light` 底线，字重 600；`h1` 1.55em、`h2` 1.3em、`h3` 1.12em，上下 1.25em/0.55em 呼吸；标题不做居中。
- 段落/列表/表格/引用间距 0.75em；列表左缩进 1.45em，`li + li` 上距 0.25em；引用块左侧 1px `border` 竖线 + 1em 左内边距 + secondary 文字。
- 行内 code：`panel-muted` 底、4px 圆角、等宽 0.88em、可任意折行。
- 代码块：整块 `panel-muted` 底 + 1px `border-light` 边、10px 圆角；头部条与代码区分离（头部语言标签 12px tertiary + 复制按钮，hover 浅灰）；代码 0.88em 等宽、可横向滚动。
- 表格：套 1px `border-light` 圆角容器（`markdown-table-wrap`）横向滚动；表头 secondary 13px 600；单元格细分隔线 `border-bottom`，数字列 `tabular-nums`。
- 公式（KaTeX）宽松模式渲染，展示型公式容器可横向滚动；Mermaid 图自适应宽度 `max-width: 100%`、高度 auto。
- 图片：块级、居中、`max-width: 100%`、`height: auto`。
- 底部 `hr` 分隔线 1px `border-light`。

### 7.8 横幅（inline alerts）

错误、上下文警告、阻塞提示统一出现在消息流顶部（输入坞之上）、内容列之内：`mx-4 mt-2`、圆角 10px、内边距 `px-4 py-2`、`flex items-center gap-2`，**14px 图标 + 14px 文本**，可带一个关闭/查看按钮（ghost、xs）。

| 场景 | 底 / 文字 | 图标 |
|---|---|---|
| 请求错误 | `error-muted` / error | alert-circle |
| 上下文已满 | `error-muted` / error | alert-octagon |
| 已自动压缩 | warning 10% / warning（或 amber 对） | minimize |
| 接近上限 | warning 10% / warning | alert-triangle |
| 输入阻断提示 | `warning-muted` / warning | 文字即可 |

顶部横幅用透明度着色（`10%`）与图标变体区分场景；颜色语义永远对应动作性质（错误=error、提醒=warning），不引入情绪化新色。

### 7.9 消息流滚动

- 消息列表内部滚动；用户位于底部（距底 64px 内）时新消息/流式增量自动滚到底部；用户上翻阅读时不打断。
- 离开底部时显示悬浮「回到底部」圆形按钮（fixed 于右下角、`z-20`）：40px 圆、`control` 底、1px `border-light`（该按钮悬浮于内容流之上，允许细边以区分背景，属于 1.1 规则 5 的窄例外）、hover 浅灰加深，点击平滑滚回底部。
- 进入对话时首帧滚到最新消息；移动端列表底部为底部浮航让位。
- 列表不显示滚动条审美化处理：窄屏消息区无自定义滚动条宽度问题（横向 chip 区隐藏滚动条但保留滚动）。

### 7.10 上下文预算（长对话健康度）

用量对用户保持可见但不打扰阅读：

- 移动端：消息流下方（消息区与输入坞之间，`px-4 py-1.5` + 1px 顶部分隔线）常驻一行微型用量条：4px 进度条 + 等宽文本「N / M tokens · K 缓存命中」+ 右侧百分比；≥75% 进度条转 warning、≥90% 转 error。
- 桌面端：聊天主区不常驻用量条（保持正文安静），在详情面可用环形进度（48px 圆环 + 文本）呈现；聊天区内靠 7.8 横幅承担提示职责。
- 满/近满时以 7.8 横幅提示「新建对话或输入 /compact」——不自动裁剪历史，把决定权交给用户（压缩动作完成后也以横幅回执）。

---

## 8. 页面级模式速查（工作台通用，非聊天专属）

### 8.1 工作台骨架

- 单屏：`h-dvh`，无整页滚动；导航 256px（桌面）/ 抽屉（移动）+ 弹性主区。
- 主内容 view 切换用 200ms 淡入（`view-enter`）；移动端顶部分留 64px 给浮航。
- 顶栏只在项目详情等二级面存在：`panel` 底 + 与正文区 0 分隔（无边框无投影），高度 ~48px。

### 8.2 列表页

- 列表不用分隔线：`space-y-1`（4px）分段 + 行 hover 浅灰整块填充；行圆角 10px；行内高 32px（紧凑列表）~80px（项目卡片行）；更多操作按钮桌面端 hover 才出现（opacity 切换），移动端常驻。
- 组与行的区分用「分组头 28px 行 + 4px 间距」，不用分割线。
- 页面标题 24px 600 负字距；副标 14px secondary。

### 8.3 左右分栏详情

- 弹窗内分栏：左导航 208px 无边框（选项行 `py-2 px-3`、14px、选中 `interaction-active` 底），右内容区与导航以背景差（bg vs panel）区分而非竖线；右栏内部区块以 `border-light` 下分隔线 + 16px 600 标题组织。
- 统计卡：`project-control` 底、圆角 18px、内距 16px；数值 24px 600 `tabular-nums`。

---

## 9. 无障碍与交互细节

- **焦点**：链接/按钮/`role=button` 的 `focus-visible` = `accent-soft` 背景填充（替代 ring/outline，全局 `:where(a, button, [role=button])` 已兜底）；输入类 = 强调色光标。焦点元素样式必须是可见的但非描边。
- **命中区**：移动端一切按钮与链接 ≥44×44px（全局兜底实现），桌面端按钮 ≥32px、图标钮 ≥28px、列表行 ≥32px 高。
- **对比度**：正文 primary/secondary 相对底色 ≥4.5:1；tertiary 仅用于 12px 辅助信息且 ≥3:1；强调蓝在浅色主题用于正文时选用 accent 深一号（hover 值）保证达标；focus 底 accent-soft 上的文字 ≥3.5:1。
- **状态不止颜色**：成功/失败/进行中除颜色外必须提供图标或文字形态差异（如进度徽章带 label、错误横幅带 icon + 文案）。
- **动效降级**：`prefers-reduced-motion: reduce` 下禁用全部动画与位移过渡，只保留 160ms 淡入；滚动平滑等行为不降级。
- **语义与朗读**：加载指示器 `role="status"` + `aria-live="polite"`；可折叠过程面板用 `aria-expanded`；纯图标按钮带 `aria-label`；消息流 `aria-label` 说明作用域。
- **键盘**：完整 Tab 序 + 折叠/菜单/弹窗原生键盘交互（Radix 语义）；聊天正文区支持 Enter 发送 / Shift+Enter 换行的键位在可见位置说明（占位文字或 Tooltip）。
- **安全区**：任何贴边控件（输入坞、底部弹窗、移动浮航）吃满 `env(safe-area-inset-bottom)`。

---

## 10. 落地实现建议（Token 化清单）

把本规范落地到 Tailwind v4 + CSS 变量工程时的最小清单：

1. 在 `:root` / `.dark` 定义 2.2–2.7 的 `--color-*` 令牌（全部 OKLCH），用 `@theme inline` 映射工具类；组件代码只写 `bg-[var(--color-*)]` 或映射后的类名，禁止出现十六进制。
2. 定义圆角令牌并**明确覆盖默认值**（本项目 `--radius-md=10px`、`--radius-lg=14px` 偏离 Tailwind 默认），全组统一。
3. 阴影令牌默认 `none`，只给 `--shadow-pill` / `--shadow-input` 两个正资产；「要悬浮感先考虑背景差，阴影是最后手段」。
4. 动效令牌：时长 150/200/300/400ms 四档 + `ease-out-quart` 与 `cubic-bezier(0.23,1,0.32,1)` 等既有缓动；所有动画套 `prefers-reduced-motion` 降级。
5. 触控兜底：全局媒体查询把移动端按钮与链接压到 44px 最小命中区。
6. 焦点兜底：全局 `:where(a,button,[role=button]):focus-visible` 用背景填充，替代所有局部 ring 代码。
7. 按钮组件导出统一 `buttonVariants`（cva）：变体 = 5.2 表，尺寸 = 5.3 表，禁止在调用点另写样式覆盖颜色/圆角。
8. 聊天区组件拆为：消息流列表（滚动锚定逻辑独立）、气泡（用户）、正文渲染器（Markdown 画布）、输入坞（唯一输入入口）、过程条（状态折叠）、横幅（inline alerts 统一表）；输入坞不随对话形态换组件。
9. 文案纪律：UI 一律简体中文短句（占位如「问点什么」），加载/状态行用名词短语，错误信息给原因 + 动作，不给裸错误码。

---

## 附录：令牌速查（两主题核心值）

| 令牌 | 浅色 | 深色 |
|---|---|---|
| `--color-bg` | `oklch(1 0 0)` | `oklch(0.13 0 0)` |
| `--color-surface` | `oklch(0.985 0 0)` | `oklch(1 0 0 / 0.055)` |
| `--color-panel-muted` | `oklch(0.965 0 0)` | `oklch(1 0 0 / 0.07)` |
| `--color-surface-hover` | `oklch(0.145 0 0 / 0.05)` | `oklch(1 0 0 / 0.08)` |
| `--color-surface-active` | `oklch(0.145 0 0 / 0.08)` | `oklch(1 0 0 / 0.12)` |
| `--color-text-primary` | `oklch(0.18 0 0)` | `oklch(0.95 0 0)` |
| `--color-text-secondary` | `oklch(0.42 0 0)` | `oklch(0.76 0 0)` |
| `--color-text-tertiary` | `oklch(0.56 0 0)` | `oklch(0.66 0 0)` |
| `--color-accent` | `oklch(0.5761 0.2508 258.23)` | `oklch(0.7308 0.1583 248.13)` |
| `--color-accent-hover` | `oklch(0.5151 0.2399 257.85)` | `oklch(0.78 0.13 248.13)` |
| `--color-accent-muted` | `oklch(0.9158 0.0473 245.12)` | `oklch(0.32 0.07 253)` |
| `--color-accent-soft` | `oklch(0.9458 0.0293 249.85)` | `oklch(0.28 0.055 253)` |
| `--color-border` | `oklch(0.145 0 0 / 0.14)` | `oklch(1 0 0 / 0.16)` |
| `--color-border-light` | `oklch(0.145 0 0 / 0.08)` | `oklch(1 0 0 / 0.1)` |
| `--color-overlay` | `oklch(0.145 0 0 / 0.28)` | `oklch(0.05 0.006 255 / 0.48)` |
| `--radius-sm/md/lg/xl` | 6 / 10 / 14 / 18px（两主题一致） | 同左 |
| `--shadow-pill` | `0 1px 2px rgba(0,0,0,.05), 0 4px 12px rgba(0,0,0,.04)` | 加深一倍 |
| `--shadow-input` | `0 1px 2px rgba(0,0,0,.04), 0 8px 24px rgba(0,0,0,.035)` | 加深一倍 |

> 表中 sRGB 近似值（第 2 章括号内）由 OKLCH 换算，仅用于设计工具内预览；代码实现一律写 OKLCH 原值。
