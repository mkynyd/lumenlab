# light-ai-chat (LumenLab) 接口文档

## 概述

- **项目名称**: LumenLab (light-ai-chat)
- **用途**: AI 对话与项目协作平台，支持多模型对话、文件上传解析、RAG 检索增强、文档转换、Artifact 管理等
- **技术栈**: Next.js 16 App Router + TypeScript + Prisma (PostgreSQL + pgvector) + Redis
- **认证方式**: NextAuth v5 (Auth.js) JWT Session
- **Base URL**: `https://lab.mkynstudio.top`
- **Content-Type**: `application/json`（除特殊说明外）

---

## 认证说明

### 认证流程

1. 用户通过 `/login` 页面提交 email + password
2. 后端通过 NextAuth Credentials Provider 验证
3. 密码使用 bcrypt (cost 12) 验证
4. 验证成功后签发 JWT，存储在 httpOnly Cookie 中

### 公开路由

以下路由无需登录即可访问：
- `/home` — 首页
- `/docs` — 文档页
- `/login` — 登录页
- `/register` — 注册页
- `/api/auth/*` — NextAuth 内部处理器
- `/api/auth/register` — 注册接口
- `/api/health` — 健康检查

### 受保护路由

除上述公开路由外，所有页面和 API 均需要登录。未认证时：
- 页面路由：重定向到 `/home`
- API 路由：返回 `{"error": "Unauthorized"}`，HTTP 状态码 **401**

### 速率限制

所有速率限制基于滑动窗口算法实现（优先 Redis Lua 脚本，降级内存实现）。

| 类型 | 限制 | 适用范围 |
|------|------|---------|
| 登录 | 5 次/60 秒（按 email + IP） | POST `/api/auth/[...nextauth]` |
| 注册 | 3 次/60 秒（按 IP） | POST `/api/auth/register` |
| 对话 | 30 次/60 秒 | POST `/api/chat` |
| API Key 管理 | 10 次/60 秒 | POST `/api/user/api-keys` |
| 文件上传 | 20 次/60 秒 | POST `/api/projects/[id]/files` |
| 文件批处理 | 10 次/60 秒 | POST `/api/projects/[id]/files/batch` |

触发限流返回 HTTP 状态码 **429**。

---

## 接口列表

### 1. 认证

#### POST `/api/auth/[...nextauth]`

NextAuth.js 标准处理器。

**Method**: `GET` | `POST`

**认证**: 无需（由 NextAuth 内部处理）

**请求参数** (POST):

```json
{
  "email": "string（必填）",
  "password": "string（必填）"
}
```

**响应**: NextAuth 标准响应，设置 session Cookie。

---

#### POST `/api/auth/register`

用户注册。

**Method**: `POST`

**认证**: 无需

**速率限制**: 3 次/60 秒（按 IP）

**请求参数**:

```json
{
  "email": "string（必填，邮箱地址）",
  "password": "string（必填，密码）",
  "registrationCode": "string（必填，注册码）"
}
```

**处理流程**:

1. Zod schema 验证
2. bcrypt (cost 12) 哈希密码
3. 验证注册码：
   - 查询 `RegistrationCode` 表
   - 校验注册码是否存在且状态为 `active`
   - 校验是否过期
   - 校验是否超过最大兑换次数
4. 创建用户 + 兑换记录 (`RegistrationRedemption`) 在同一事务中
5. 新用户自动关联注册码对应的 `credentialProfileId`

**成功响应** (201):

```json
{
  "success": true,
  "user": {
    "id": "string",
    "email": "string",
    "name": "string | null"
  }
}
```

**错误响应**:

| 状态码 | 说明 |
|--------|------|
| 400 | 请求参数验证失败或注册码无效 |
| 409 | 邮箱已被注册 |
| 429 | 注册频率超限 |

---

### 2. 健康检查

#### GET `/api/health`

系统健康检查。

**Method**: `GET`

**认证**: 无需

**请求参数**: 无

**成功响应** (200):

```json
{
  "status": "healthy",
  "timestamp": "ISO 8601 字符串",
  "checks": {
    "database": "ok | error",
    "redis": "ok | error | not_configured"
  }
}
```

**降级响应** (200):

部分组件异常但服务仍可用，`status` 为 `"degraded"`。

**故障响应** (503):

`status` 为 `"unhealthy"`。

---

### 3. 对话 (Chat)

#### POST `/api/chat`

核心对话接口，发送消息并获取 AI 流式回复。

**Method**: `POST`

**认证**: 需要（手动调用 `auth()`）

**速率限制**: 30 次/60 秒

**请求参数**:

```json
{
  "conversationId": "string | undefined（可选，已有对话 ID）",
  "projectId": "string | undefined（可选，项目 ID）",
  "message": "string（必填，用户消息内容）",
  "model": "string（可选，模型选择，如 claude-sonnet-4-6 等）",
  "thinkingEnabled": "boolean（可选，是否启用思考模式）",
  "skillId": "string（可选，激活的技能 ID）"
}
```

**响应**:

Content-Type: `text/event-stream`（SSE 流式响应）

流式事件类型：
- `text` — 逐 token 文本输出
- `reasoning` — 推理过程（thinking 模式）
- `tool_call` — 工具调用请求
- `tool_result` — 工具执行结果
- `sources` — 引用的来源
- `error` — 错误信息
- `done` — 流结束

**错误响应**:

| 状态码 | 说明 |
|--------|------|
| 400 | 请求参数验证失败 |
| 401 | 未登录 |
| 429 | 对话频率超限 |

---

#### POST `/api/chat/compact`

手动压缩对话历史（调用 DeepSeek API 对历史消息进行摘要）。

**Method**: `POST`

**认证**: 需要

**请求参数**:

```json
{
  "conversationId": "string（必填，对话 ID）",
  "prompt": "string（可选，自定义摘要指令）"
}
```

**处理流程**:

1. 校验用户是对话所有者
2. 调用 DeepSeek API 对对话历史生成摘要
3. 将摘要作为 `subtype: "context-summary"` 的系统消息插入对话

**成功响应** (200):

```json
{
  "success": true,
  "messageId": "string（摘要消息 ID）",
  "compressedCount": "number（被压缩的消息数量）",
  "tokens": "number（压缩消耗 token 数）",
  "ratio": "number（压缩比）"
}
```

---

### 4. 对话管理

#### GET `/api/conversations`

获取对话列表（最近 100 条，不含项目内对话）。

**Method**: `GET`

**认证**: 需要

**请求参数**: 无

**成功响应** (200):

```json
{
  "conversations": [
    {
      "id": "string",
      "title": "string | null",
      "model": "string",
      "modelLock": "boolean",
      "thinkingEnabled": "boolean",
      "updatedAt": "ISO 8601 字符串"
    }
  ]
}
```

---

#### GET `/api/conversations/[id]`

获取单个对话详情，包含所有消息。

**Method**: `GET`

**认证**: 需要

**路径参数**:

| 参数 | 类型 | 说明 |
|------|------|------|
| `id` | string | 对话 ID |

**成功响应** (200):

```json
{
  "conversation": {
    "id": "string",
    "title": "string | null",
    "model": "string",
    "modelLock": "boolean",
    "thinkingEnabled": "boolean",
    "projectId": "string | null",
    "createdAt": "ISO 8601 字符串",
    "updatedAt": "ISO 8601 字符串",
    "messages": [
      {
        "id": "string",
        "role": "user | assistant | system",
        "content": "string",
        "reasoningContent": "string | null",
        "subtype": "string | null",
        "tokenCount": "number",
        "cacheHitTokens": "number",
        "cacheMissTokens": "number",
        "sources": "object | null",
        "createdAt": "ISO 8601 字符串"
      }
    ]
  }
}
```

---

#### DELETE `/api/conversations/[id]`

删除对话及其所有消息。

**Method**: `DELETE`

**认证**: 需要

**路径参数**:

| 参数 | 类型 | 说明 |
|------|------|------|
| `id` | string | 对话 ID |

**成功响应** (200):

```json
{ "success": true }
```

---

### 5. 项目管理

#### GET `/api/projects`

获取用户的所有项目列表。

**Method**: `GET`

**认证**: 需要

**请求参数**: 无

**成功响应** (200):

```json
{
  "projects": [
    {
      "id": "string",
      "name": "string",
      "description": "string | null",
      "type": "experiment | review | coding | general",
      "defaultModel": "string",
      "thinkingEnabled": "boolean",
      "createdAt": "ISO 8601 字符串",
      "updatedAt": "ISO 8601 字符串",
      "_count": {
        "conversations": "number",
        "files": "number",
        "artifacts": "number"
      }
    }
  ]
}
```

---

#### POST `/api/projects`

创建新项目。

**Method**: `POST`

**认证**: 需要

**请求参数**:

```json
{
  "name": "string（必填，项目名称）",
  "description": "string（可选，项目描述）",
  "type": "\"experiment\" | \"review\" | \"coding\" | \"general\"（必填，项目类型）",
  "defaultModel": "string（可选，默认模型）",
  "thinkingEnabled": "boolean（可选，是否默认开启思考模式）",
  "quickActions": [
    {
      "title": "string（快捷操作标题）",
      "prompt": "string（快捷操作 Prompt）"
    }
  ]
}
```

**处理流程**:

1. 校验用户仍存在于数据库
2. 创建项目
3. 创建默认系统快捷操作 + 用户提供的快捷操作

**成功响应** (201):

```json
{
  "project": {
    "id": "string",
    "name": "string",
    "type": "string",
    "defaultModel": "string",
    "thinkingEnabled": "boolean"
  }
}
```

---

#### GET `/api/projects/[id]`

获取项目详情（含文件、对话、快捷操作列表）。

**Method**: `GET`

**认证**: 需要

**路径参数**:

| 参数 | 类型 | 说明 |
|------|------|------|
| `id` | string | 项目 ID |

**成功响应** (200):

```json
{
  "project": {
    "id": "string",
    "name": "string",
    "description": "string | null",
    "type": "experiment | review | coding | general",
    "defaultModel": "string",
    "thinkingEnabled": "boolean",
    "systemPrompt": "string | null",
    "createdAt": "ISO 8601 字符串",
    "updatedAt": "ISO 8601 字符串",
    "files": [
      {
        "id": "string",
        "originalName": "string",
        "status": "parsing | parsed | enhanced | failed",
        "category": "string",
        "createdAt": "ISO 8601 字符串"
      }
    ],
    "conversations": [
      {
        "id": "string",
        "title": "string | null",
        "updatedAt": "ISO 8601 字符串"
      }
    ],
    "quickActions": [
      {
        "id": "string",
        "title": "string",
        "prompt": "string",
        "sortOrder": "number",
        "isSystem": "boolean"
      }
    ],
    "_count": {
      "conversations": "number",
      "files": "number",
      "artifacts": "number"
    }
  }
}
```

---

#### PATCH `/api/projects/[id]`

更新项目信息。

**Method**: `PATCH`

**认证**: 需要

**路径参数**:

| 参数 | 类型 | 说明 |
|------|------|------|
| `id` | string | 项目 ID |

**请求参数**（所有字段可选）:

```json
{
  "name": "string（可选，项目名称）",
  "description": "string（可选，项目描述）",
  "type": "\"experiment\" | \"review\" | \"coding\" | \"general\"（可选）",
  "defaultModel": "string（可选）",
  "thinkingEnabled": "boolean（可选）",
  "systemPrompt": "string（可选，系统提示词）"
}
```

**成功响应** (200):

```json
{
  "project": { "..." }
}
```

---

#### DELETE `/api/projects/[id]`

删除项目及其关联数据。

**Method**: `DELETE`

**认证**: 需要

**路径参数**:

| 参数 | 类型 | 说明 |
|------|------|------|
| `id` | string | 项目 ID |

**处理流程**:

1. 删除项目中所有文件的对象存储数据（含 resources）
2. 删除所有对话
3. 在事务中删除项目记录

**成功响应** (200):

```json
{ "success": true }
```

---

### 6. 项目文件管理

#### GET `/api/projects/[id]/files`

获取项目文件列表。

**Method**: `GET`

**认证**: 需要

**路径参数**:

| 参数 | 类型 | 说明 |
|------|------|------|
| `id` | string | 项目 ID |

**成功响应** (200):

```json
{
  "files": [
    {
      "id": "string",
      "originalName": "string",
      "category": "string",
      "status": "parsing | parsed | enhanced | failed",
      "fileSize": "number",
      "mimeType": "string",
      "hasTextContent": "boolean",
      "parser": "string | null",
      "truncated": "boolean",
      "enhancementStatus": "idle | enhancing | enhanced | failed | null",
      "createdAt": "ISO 8601 字符串"
    }
  ]
}
```

---

#### POST `/api/projects/[id]/files`

上传文件到项目。

**Method**: `POST`

**认证**: 需要

**速率限制**: 20 次/60 秒

**Content-Type**: `multipart/form-data`

**路径参数**:

| 参数 | 类型 | 说明 |
|------|------|------|
| `id` | string | 项目 ID |

**FormData 字段**:

| 字段 | 类型 | 说明 |
|------|------|------|
| `file` | File | 单个文件（与 `files[]` 二选一） |
| `files[]` | File[] | 批量文件（与 `file` 二选一） |
| `category` | string | 文件分类标签 |

**处理流程**:

1. 校验每个文件（类型、大小等）
2. 上传到对象存储（七牛 Kodo 或本地文件系统）
3. 创建 `FileAsset` 记录，初始状态 `parsing`
4. 触发解析任务流水线

**成功响应** (201):

```json
{
  "files": [
    {
      "id": "string",
      "originalName": "string",
      "status": "parsing",
      "category": "string"
    }
  ],
  "errors": ["string（失败的文件错误信息）"],
  "summary": {
    "total": "number",
    "succeeded": "number",
    "failed": "number"
  }
}
```

---

#### POST `/api/projects/[id]/files/batch`

批处理项目文件。

**Method**: `POST`

**认证**: 需要

**速率限制**: 10 次/60 秒

**路径参数**:

| 参数 | 类型 | 说明 |
|------|------|------|
| `id` | string | 项目 ID |

**请求参数**:

```json
{
  "action": "\"delete\" | \"reparse\" | \"download\"（必填，操作类型）",
  "fileIds": ["string（文件 ID 数组）"],
  "category": "string（可选，与 action=\"download\"一起使用时批量更新分类）"
}
```

**各 action 的响应**:

- **delete** (200):
  ```json
  { "success": true }
  ```
  删除文件、分块、对象存储数据，刷新项目索引。

- **reparse** (200):
  ```json
  { "success": true }
  ```
  重新将文件加入解析队列。

- **download** (200):
  ```json
  { "content": "string（所有文件合并后的 Markdown 内容）" }
  ```

---

### 7. 项目 Artifact 管理

#### GET `/api/projects/[id]/artifacts`

获取项目下的 Artifact 列表（摘要，不含 content）。

**Method**: `GET`

**认证**: 需要

**路径参数**:

| 参数 | 类型 | 说明 |
|------|------|------|
| `id` | string | 项目 ID |

**成功响应** (200):

```json
{
  "artifacts": [
    {
      "id": "string",
      "title": "string",
      "type": "string",
      "createdAt": "ISO 8601 字符串",
      "updatedAt": "ISO 8601 字符串"
    }
  ]
}
```

---

#### POST `/api/projects/[id]/artifacts`

在项目下创建 Artifact。

**Method**: `POST`

**认证**: 需要

**路径参数**:

| 参数 | 类型 | 说明 |
|------|------|------|
| `id` | string | 项目 ID |

**请求参数**:

```json
{
  "title": "string（可选，标题）",
  "type": "string（必填，Artifact 类型，共 13 种可选）",
  "content": "string（必填，内容）",
  "conversationId": "string（可选，关联的对话 ID）",
  "messageId": "string（可选，关联的消息 ID，若提供则使用该消息的 content）"
}
```

**Artifact 类型**（13 种）:
`report`, `code`, `essay`, `presentation`, `email`, `proposal`, `analysis`, `summary`, `plan`, `specification`, `documentation`, `tutorial`, `other`

**成功响应** (201):

```json
{
  "artifact": {
    "id": "string",
    "title": "string",
    "type": "string",
    "content": "string",
    "createdAt": "ISO 8601 字符串"
  }
}
```

---

### 8. 项目快捷操作

#### GET `/api/projects/[id]/quick-actions`

获取项目快捷操作列表。

**Method**: `GET`

**认证**: 需要

**成功响应** (200):

```json
{
  "quickActions": [
    {
      "id": "string",
      "title": "string",
      "prompt": "string",
      "sortOrder": "number",
      "isSystem": "boolean"
    }
  ]
}
```

---

#### POST `/api/projects/[id]/quick-actions`

创建快捷操作。

**Method**: `POST`

**认证**: 需要

**请求参数**:

```json
{
  "title": "string（必填，标题）",
  "prompt": "string（必填，Prompt 模板）"
}
```

**成功响应** (201):

```json
{
  "quickAction": {
    "id": "string",
    "title": "string",
    "prompt": "string",
    "sortOrder": "number"
  }
}
```

---

#### PATCH `/api/projects/[id]/quick-actions/[qid]`

更新快捷操作。

**Method**: `PATCH`

**认证**: 需要

**路径参数**:

| 参数 | 类型 | 说明 |
|------|------|------|
| `id` | string | 项目 ID |
| `qid` | string | 快捷操作 ID |

**请求参数**（所有字段可选）:

```json
{
  "title": "string（可选）",
  "prompt": "string（可选）",
  "sortOrder": "number（可选）"
}
```

**成功响应** (200):

```json
{
  "quickAction": { "..." }
}
```

---

#### DELETE `/api/projects/[id]/quick-actions/[qid]`

删除快捷操作。

**Method**: `DELETE`

**认证**: 需要

> 注意：系统预设快捷操作不可删除。

**成功响应** (200):

```json
{ "success": true }
```

---

#### POST `/api/projects/[id]/quick-actions/batch`

批量创建快捷操作。

**Method**: `POST`

**认证**: 需要

**请求参数**:

```json
{
  "actions": [
    {
      "title": "string",
      "prompt": "string"
    }
  ]
}
```

**成功响应** (201):

```json
{ "count": "number（创建数量）" }
```

---

#### POST `/api/projects/[id]/quick-actions/generate`

根据描述使用 AI 生成快捷操作。

**Method**: `POST`

**认证**: 需要

**请求参数**:

```json
{
  "description": "string（必填，描述希望的快捷操作功能）"
}
```

**处理流程**:

1. 使用 DeepSeek API 根据描述生成标题和 Prompt
2. 创建快捷操作记录

**成功响应** (200):

```json
{
  "quickAction": {
    "title": "string",
    "prompt": "string"
  }
}
```

---

### 9. 项目系统提示词生成

#### POST `/api/projects/[id]/generate-prompt`

使用 AI 生成项目级的系统提示词和推荐快捷操作。

**Method**: `POST`

**认证**: 需要

**请求参数**:

```json
{
  "userInput": "string（必填，用户对项目目标的描述）",
  "mode": "string（必填，生成模式）"
}
```

**处理流程**:

1. 使用 LLM 根据用户输入生成系统提示词和快捷操作
2. 自动更新项目的 `systemPrompt` 字段

**成功响应** (200):

```json
{
  "systemPrompt": "string",
  "quickActions": [
    {
      "title": "string",
      "prompt": "string"
    }
  ]
}
```

---

### 10. 项目向量库

#### GET `/api/projects/[id]/vector-library`

获取项目文件的向量库图谱。

**Method**: `GET`

**认证**: 需要

**路径参数**:

| 参数 | 类型 | 说明 |
|------|------|------|
| `id` | string | 项目 ID |

**成功响应** (200):

```json
{
  "graph": {
    "nodes": [],
    "edges": []
  }
}
```

> 图谱基于项目文件及其文档分块构建。

---

### 11. 文件管理（全局）

#### GET `/api/files/[id]`

获取单个文件详情（含 resources）。

**Method**: `GET`

**认证**: 需要

**路径参数**:

| 参数 | 类型 | 说明 |
|------|------|------|
| `id` | string | 文件 ID |

**成功响应** (200):

```json
{
  "file": {
    "id": "string",
    "originalName": "string",
    "status": "parsing | parsed | enhanced | failed",
    "category": "string",
    "fileSize": "number",
    "mimeType": "string",
    "hasTextContent": "boolean",
    "textContent": "string | null",
    "parser": "string | null",
    "truncated": "boolean",
    "enhancementStatus": "idle | enhancing | enhanced | failed | null",
    "projectId": "string",
    "resources": [
      {
        "id": "string",
        "originalName": "string",
        "mimeType": "string"
      }
    ],
    "createdAt": "ISO 8601 字符串",
    "updatedAt": "ISO 8601 字符串"
  }
}
```

---

#### PATCH `/api/files/[id]`

更新文件信息。

**Method**: `PATCH`

**认证**: 需要

**请求参数**（所有字段可选）:

```json
{
  "textContent": "string（可选，手动设置文本内容）",
  "category": "string（可选，更新分类标签）"
}
```

**成功响应** (200):

```json
{
  "success": true,
  "enhancementStatus": "string"
}
```

---

#### DELETE `/api/files/[id]`

删除文件及其关联数据。

**Method**: `DELETE`

**认证**: 需要

**成功响应** (200):

```json
{ "success": true }
```

---

#### GET `/api/files/[id]/download`

下载文件。

**Method**: `GET`

**认证**: 需要

**处理**:
- 七牛 Kodo 存储: 返回 `{ "url": "签名下载 URL" }`（HTTP 200）
- 本地存储: 直接返回二进制文件流

---

#### POST `/api/files/[id]/enhance`

使用 AI 增强文件文本内容（OCR/解析结果结构化优化）。

**Method**: `POST`

**认证**: 需要

**处理流程**:

1. 调用 DeepSeek API 对解析出的文本进行结构化增强
2. 更新 `enhancementStatus` 为 `enhancing` -> `enhanced` 或 `failed`

**成功响应** (200):

```json
{
  "success": true,
  "enhancementStatus": "enhanced",
  "enhancedAt": "ISO 8601 字符串",
  "summary": "string（增强摘要）"
}
```

---

#### POST `/api/files/[id]/parse`

触发文件重新解析。

**Method**: `POST`

**认证**: 需要

**处理**: 将文件重新加入解析任务队列（最长 300 秒）

**成功响应** (200):

```json
{
  "file": {
    "id": "string",
    "status": "parsing",
    "hasTextContent": "boolean",
    "parser": "string | null",
    "truncated": "boolean"
  }
}
```

---

#### GET `/api/files/[id]/resources/[resourceId]`

获取文件关联的资源图片。

**Method**: `GET`

**认证**: 需要

**响应**: 图片二进制数据（从对象存储获取）

---

#### POST `/api/files/cleanup-stale`

清理卡住的解析任务（将解析超过 10 分钟的文件标记为 `failed`）。

**Method**: `POST`

**认证**: 需要

**成功响应** (200):

```json
{ "cleaned": "number" }
```

---

### 12. Artifact 管理（全局）

#### GET `/api/artifacts/[id]`

获取单个 Artifact 详情。

**Method**: `GET`

**认证**: 需要

**成功响应** (200):

```json
{
  "artifact": {
    "id": "string",
    "title": "string",
    "type": "string",
    "content": "string",
    "projectId": "string | null",
    "conversationId": "string | null",
    "messageId": "string | null",
    "createdAt": "ISO 8601 字符串",
    "updatedAt": "ISO 8601 字符串"
  }
}
```

---

#### PATCH `/api/artifacts/[id]`

更新 Artifact。

**Method**: `PATCH`

**认证**: 需要

**请求参数**（所有字段可选）:

```json
{
  "title": "string（可选）",
  "type": "string（可选）",
  "content": "string（可选）"
}
```

**成功响应** (200):

```json
{
  "artifact": { "..." }
}
```

---

#### DELETE `/api/artifacts/[id]`

删除 Artifact。

**Method**: `DELETE`

**认证**: 需要

**成功响应** (200):

```json
{ "success": true }
```

---

#### GET `/api/artifacts/[id]/export`

导出 Artifact 为文件。

**Method**: `GET`

**认证**: 需要

**查询参数**:

| 参数 | 类型 | 说明 |
|------|------|------|
| `format` | string | 导出格式：`markdown` / `docx` / `pdf` |

**处理流程**:

1. 使用 Redis 缓存导出结果（最长 120 秒生成时间）
2. 根据格式调用对应的导出转换逻辑

**响应**: 二进制文件流（Content-Type 取决于格式）

---

### 13. Agent 工具审批

#### POST `/api/agent/approve`

批准 Agent 工具执行请求。

**Method**: `POST`

**认证**: 需要

**请求参数**:

```json
{
  "token": "string（必填，一次性审批令牌）",
  "executionId": "string（必填，工具执行 ID）",
  "scope": "\"once\" | \"session\"（可选，审批范围，默认为 once）"
}
```

**处理流程**:

1. 校验审批令牌有效性
2. 重新校验工具/技能可用性和策略
3. 消费一次性审批令牌
4. 执行工具并记录审计事件
5. L3/L4 风险等级的工具不支持 `session` 范围审批

**成功响应** (200):

```json
{
  "ok": true,
  "status": "approved",
  "scope": "once | session",
  "executionId": "string",
  "resultSummary": "string | null"
}
```

---

#### POST `/api/agent/reject`

拒绝 Agent 工具执行请求。

**Method**: `POST`

**认证**: 需要

**请求参数**:

```json
{
  "executionId": "string（必填，工具执行 ID）",
  "reason": "string（可选，拒绝原因）"
}
```

**处理流程**:

1. 将执行记录标记为 `rejected`
2. 记录审计事件

**成功响应** (200):

```json
{
  "ok": true,
  "executionId": "string"
}
```

---

### 14. 用户管理

#### GET `/api/user/profile`

获取当前用户信息。

**Method**: `GET`

**认证**: 需要

**成功响应** (200):

```json
{
  "email": "string",
  "name": "string | null",
  "avatarPreset": "string | null",
  "avatarUrl": "string | null"
}
```

---

#### PATCH `/api/user/profile`

更新用户信息。

**Method**: `PATCH`

**认证**: 需要

**请求参数**（所有字段可选）:

```json
{
  "name": "string（可选，昵称）",
  "avatarPreset": "string（可选，头像预设标识）"
}
```

**成功响应** (200): 用户信息对象。

---

#### GET `/api/user/profile/avatar`

获取用户头像。

**Method**: `GET`

**认证**: 需要

**响应**: 头像图片或七牛签名 URL 重定向。

---

#### POST `/api/user/profile/avatar`

上传用户头像。

**Method**: `POST`

**认证**: 需要

**Content-Type**: `multipart/form-data`

**FormData 字段**:

| 字段 | 类型 | 说明 |
|------|------|------|
| `avatar` | File | 头像图片文件（JPG/PNG/WebP，最大 20MB） |

**处理流程**:

1. 删除旧头像文件
2. 上传新头像到对象存储
3. 支持七牛图片样式转换

**成功响应** (200):

```json
{
  "email": "string",
  "name": "string | null",
  "avatarPreset": "string | null",
  "avatarUrl": "string"
}
```

---

#### POST `/api/user/api-keys`

设置用户自定义供应商 API Key。

**Method**: `POST`

**认证**: 需要

**前置条件**: `USER_API_KEYS_ENABLED` 环境变量为 `true`

**速率限制**: 10 次/60 秒

**请求参数**:

```json
{
  "provider": "\"deepseek\" | \"minimax\" | \"mineru\" | \"bailian\"（必填）",
  "apiKey": "string（必填，API Key）"
}
```

**处理流程**:

1. Key 使用 AES-256-GCM 加密存储
2. 每个 provider 每个用户仅可存储一个 key（upsert 模式）

**成功响应** (200):

```json
{
  "provider": "string",
  "keyPrefix": "string（脱敏前缀）"
}
```

---

#### POST `/api/user/generate-profile`

使用 AI 生成用户个人资料提示词。

**Method**: `POST`

**认证**: 需要

**请求参数**:

```json
{
  "nickname": "string（必填，昵称）",
  "profession": "string（必填，职业）",
  "details": "string（必填，补充说明）"
}
```

**成功响应** (200):

```json
{
  "profilePrompt": "string（生成的个人资料描述）"
}
```

---

#### POST `/api/user/switch-code`

切换用户注册码。

**Method**: `POST`

**认证**: 需要

**请求参数**:

```json
{
  "code": "string（必填，新注册码）"
}
```

**处理流程**:

1. 释放旧注册码的兑换计数
2. 在序列化事务中兑换新注册码
3. 更新用户的 `credentialProfileId`

**成功响应** (200):

```json
{ "success": true }
```

---

### 15. 用量统计

#### GET `/api/me/usage`

获取当前用户的 Token 用量统计。

**Method**: `GET`

**认证**: 需要

**请求参数**: 无

**成功响应** (200):

```json
{
  "tier": "string（用户套餐等级）",
  "cycle": {
    "start": "ISO 8601 字符串",
    "end": "ISO 8601 字符串"
  },
  "quota": "number（配额总量）",
  "usage": {
    "currentCycleCredits": "number（当前计费周期 Credits）",
    "currentCycleTokens": "number（当前计费周期 Tokens）",
    "last24hCredits": "number（近 24 小时 Credits）",
    "last7dCredits": "number（近 7 天 Credits）",
    "last5hCredits": "number（近 5 小时 Credits）",
    "modelDistribution": [
      {
        "model": "string",
        "tokens": "number",
        "percentage": "number"
      }
    ],
    "recentRecords": [
      {
        "id": "string",
        "model": "string",
        "tokens": "number",
        "createdAt": "ISO 8601 字符串"
      }
    ]
  }
}
```

---

### 16. 缓存指标

#### GET `/api/metrics/cache`

获取缓存性能指标。

**Method**: `GET`

**认证**: 需要

**查询参数**:

| 参数 | 类型 | 说明 |
|------|------|------|
| `range` | string | `"cycle"` — 按当前计费周期查询 |
| `start` | ISO 8601 | 自定义时间范围起（与 `end` 配合） |
| `end` | ISO 8601 | 自定义时间范围止（与 `start` 配合） |
| `days` | number | 按最近 N 天查询 |

**成功响应** (200):

```json
{
  "days": "number | null",
  "cycle": "object | null",
  "...metrics": {},
  "tokenUsage": {},
  "exports": {},
  "rag": {}
}
```

---

### 17. 工具：PDF 转换

#### POST `/api/tools/pdf-to-markdown`

PDF 转 Markdown 转换。

**Method**: `POST`

**认证**: 需要

**Content-Type**: `multipart/form-data`

**FormData 字段**:

| 字段 | 类型 | 说明 |
|------|------|------|
| `file` | File | PDF 文件（最大 200MB） |
| `mineruToken` | string | 可选，自定义 MinerU Token |

**响应**: `text/event-stream`（SSE 流式），实时推送转换进度。

---

#### GET `/api/tools/conversions`

获取转换历史列表。

**Method**: `GET`

**认证**: 需要

**成功响应** (200):

```json
{
  "conversions": [
    {
      "id": "string",
      "originalFileName": "string",
      "status": "string",
      "createdAt": "ISO 8601 字符串"
    }
  ]
}
```

---

#### GET `/api/tools/conversions/[id]`

获取单个转换详情（含 assets）。

**Method**: `GET`

**认证**: 需要

**成功响应** (200):

```json
{
  "conversion": {
    "id": "string",
    "originalFileName": "string",
    "status": "string",
    "markdownContent": "string | null",
    "assets": [
      {
        "id": "string",
        "originalName": "string"
      }
    ],
    "createdAt": "ISO 8601 字符串"
  }
}
```

---

#### DELETE `/api/tools/conversions/[id]`

删除转换记录。

**Method**: `DELETE`

**认证**: 需要

**成功响应** (200):

```json
{ "success": true }
```

---

#### GET `/api/tools/conversions/[id]/download`

下载转换结果 ZIP 包（包含 Markdown + PDF + 图片资源）。

**Method**: `GET`

**认证**: 需要

**处理**: 打包后缓存到对象存储。

**响应**: ZIP 文件二进制流。

---

#### GET `/api/tools/conversions/[id]/assets/[assetId]`

获取转换结果中的图片资源。

**Method**: `GET`

**认证**: 需要

**响应**: 图片二进制数据（从对象存储获取）。

---

#### POST `/api/tools/conversions/[id]/save-to-project`

将转换结果保存为项目文件。

**Method**: `POST`

**认证**: 需要

**请求参数**:

```json
{
  "projectId": "string（必填，目标项目 ID）"
}
```

**处理**: 将转换的 Markdown 内容和所有关联图片作为 `FileAsset` + `FileAssetResource` 保存到项目。

**成功响应** (200):

```json
{
  "fileId": "string",
  "projectId": "string"
}
```

---

### 18. 技能目录

#### GET `/api/skills/catalog`

获取可用技能目录。

**Method**: `GET`

**认证**: 需要

**请求参数**: 无

**成功响应** (200):

```json
{
  "categories": [
    {
      "slug": "string（分类标识，如 academic）",
      "displayName": "string（分类显示名）",
      "skills": [
        {
          "name": "string",
          "displayName": "string",
          "description": "string",
          "version": "string",
          "source": "string",
          "riskSummary": "string"
        }
      ]
    }
  ],
  "totalCount": "number"
}
```

**技能分类**: academic（学术）、exam（考试）、coding（编程）、learning（学习）、uncategorized（未分类）

---

### 19. 内部接口：注册同步

#### POST `/api/internal/registration-sync`

接收来自 course-ai-regadmin 的注册码同步数据。

**Method**: `POST`

**认证**: HMAC-SHA256 签名验证（非 JWT Session）

**请求头**:

| 头部 | 说明 |
|------|------|
| `x-sync-timestamp` | 请求时间戳（防重放） |
| `x-sync-nonce` | 随机数（防重放，5 分钟有效期） |
| `x-sync-signature` | HMAC-SHA256 签名 |

**请求体**: RSA-OAEP 加密 + AES-256-GCM 加密的载荷（由 course-ai-regadmin 构建）

**处理流程**:

1. 校验 HMAC 签名（`REGISTRATION_SYNC_SECRET`）
2. 使用 RSA 私钥（`REGISTRATION_SYNC_PRIVATE_KEY_BASE64`）解密外层
3. AES-256-GCM 解密内层快照
4. 在序列化事务中同步注册码、凭证配置和供应商凭证
5. 记录发布版本号（幂等处理）

**成功响应** (200):

```json
{
  "success": true,
  "version": "number（发布版本号）",
  "idempotent": "boolean（是否为重复请求，幂等返回 true）",
  "usage": {}
}
```

---

## 环境变量参考

| 变量名 | 说明 |
|--------|------|
| `DATABASE_URL` | PostgreSQL 数据库连接字符串 |
| `REDIS_URL` | Redis 连接字符串 |
| `AUTH_SECRET` | NextAuth 加密密钥 |
| `ENCRYPTION_KEY` | AES-256-GCM 加密密钥（API Key 静态加密） |
| `REGISTRATION_SYNC_SECRET` | 注册同步 HMAC 签名密钥 |
| `REGISTRATION_SYNC_PRIVATE_KEY_BASE64` | 注册同步 RSA 私钥（Base64） |
| `USER_API_KEYS_ENABLED` | 是否允许用户自定义 API Key |
| `QINIU_ACCESS_KEY` / `QINIU_SECRET_KEY` / `QINIU_BUCKET` / `QINIU_DOMAIN` | 七牛 Kodo 对象存储配置 |
| `DEEPSEEK_API_KEY` / `MINIMAX_API_KEY` / `MINERU_TOKEN` / `BAILIAN_API_KEY` | 供应商 API Key（服务端默认） |

---

## 数据库核心模型

### User（用户）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | UUID | 主键 |
| email | String (unique) | 邮箱 |
| passwordHash | String | bcrypt 密码哈希 |
| name | String? | 昵称 |
| avatarPreset | String? | 头像预设 |
| avatarUrl | String? | 头像 URL |
| planTier | String | 套餐等级 |
| credits | Int | 剩余 Credits |
| accessStatus | String | 访问状态 |
| credentialProfileId | UUID? | 关联的凭证配置 |

### Conversation（对话）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | UUID | 主键 |
| userId | UUID | 用户 ID |
| projectId | UUID? | 项目 ID |
| title | String? | 标题 |
| model | String | 模型 |
| modelLock | Boolean | 模型锁定 |
| thinkingEnabled | Boolean | 思考模式 |

### Message（消息）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | UUID | 主键 |
| conversationId | UUID | 对话 ID |
| role | String | user / assistant / system |
| content | Text | 内容 |
| reasoningContent | Text? | 推理过程 |
| subtype | String? | 消息子类型 |
| tokenCount | Int | Token 数 |
| cacheHitTokens | Int | 缓存命中 Token |
| cacheMissTokens | Int | 缓存未命中 Token |
| sources | Json? | 引用来源 |

### Project（项目）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | UUID | 主键 |
| userId | UUID | 用户 ID |
| name | String | 名称 |
| description | String? | 描述 |
| type | String | experiment / review / coding / general |
| defaultModel | String | 默认模型 |
| systemPrompt | Text? | 系统提示词 |

### FileAsset（文件）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | UUID | 主键 |
| projectId | UUID | 项目 ID |
| originalName | String | 原始文件名 |
| status | String | parsing / parsed / enhanced / failed |
| textContent | Text? | 解析/增强后文本 |
| enhancementStatus | String? | idle / enhancing / enhanced / failed |
| category | String | 分类标签 |

### Artifact

| 字段 | 类型 | 说明 |
|------|------|------|
| id | UUID | 主键 |
| projectId | UUID? | 项目 ID |
| userId | UUID | 用户 ID |
| title | String | 标题 |
| type | String | 类型（13 种可选） |
| content | Text | 内容 |

### RegistrationCode（注册码）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | UUID | 主键 |
| codeFingerprint | String (unique) | HMAC-SHA256 指纹 |
| encryptedCode | String | AES-256-GCM 加密码 |
| status | String | active / disabled |
| maxRedemptions | Int | 最大兑换次数 |
| expiresAt | DateTime? | 过期时间 |
| credentialProfileId | UUID? | 凭证配置 ID |

### CredentialProfile（凭证配置）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | UUID | 主键 |
| name | String | 名称 |
| status | String | active / disabled |
| version | Int | 版本号 |

### ProviderCredential（供应商凭证）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | UUID | 主键 |
| credentialProfileId | UUID | 凭证配置 ID |
| provider | String | deepseek / minimax / mineru / bailian |
| encryptedKey | String | AES-256-GCM 加密 Key |
| keyPrefix | String | 脱敏前缀 |

### RegistrationSyncNonce（同步随机数）

| 字段 | 类型 | 说明 |
|------|------|------|
| nonce | String (unique) | 随机数（防重放，5 分钟 TTL） |

### TokenUsage（Token 用量）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | UUID | 主键 |
| userId | UUID | 用户 ID |
| messageId | UUID | 消息 ID |
| model | String | 模型 |
| tokens | Int | Token 数 |
| credits | Int | Credits 消耗 |

### ToolDefinition（工具定义）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | UUID | 主键 |
| name | String | 工具名 |
| riskLevel | Int | 风险等级 (L1-L4) |
| schema | Json | 工具 JSON Schema |

### ToolExecution（工具执行记录）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | UUID | 主键 |
| status | String | pending / approved / rejected / executed |
| userId | UUID | 用户 ID |
| toolName | String | 工具名 |
| result | Json? | 执行结果 |

### ApprovalToken（审批令牌）

| 字段 | 类型 | 说明 |
|------|------|------|
| token | String (unique) | 一次性令牌 |
| executionId | UUID | 工具执行 ID |
| scope | String | once / session |
| usedAt | DateTime? | 使用时间 |

### SkillPackage（技能包）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | UUID | 主键 |
| name | String | 技能名 |
| displayName | String | 显示名 |
| description | String | 描述 |
| riskLevel | Int | 风险等级 |
| category | String | 分类 |

### DocumentChunk（文档分块，RAG）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | UUID | 主键 |
| fileId | UUID | 文件 ID |
| content | Text | 分块文本 |
| embedding | vector(1024) | pgvector 嵌入向量 |
